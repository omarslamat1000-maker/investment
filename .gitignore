# نسخ المستخدم الاحتياطية المحلية — لا تُرفع للمستودع
backups/

# ملفات النظام
.DS_Store
Thumbs.db
desktop.ini

# مخلفات أدوات التطوير
node_modules/
*.log

# ملف خط النشر — رمز الدخول الحالي بلا صلاحية workflow (أضفه يدوياً من واجهة GitHub أو بعد gh auth login)
.github/
